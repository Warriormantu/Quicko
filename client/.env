# Frontend-only environment variables (Vite)
# VITE_* prefix is required for Vite to expose to the browser

# When deployed to Vercel, set this to your Render backend URL.
# Leave empty for local development (Vite proxy in vite.config.js handles it).
VITE_API_URL=https://quicko-0paf.onrender.com